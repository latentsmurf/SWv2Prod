import { DirectorControl } from '@/components/studio/DirectorControl';

export default function DirectorPage() {
    return (
        <main className="min-h-screen bg-black p-8">
            <DirectorControl />
        </main>
    );
}
